import React from "react";

const Button = () => {
  return (
    <div>
      <button type="submit" className="btn btn-danger ">
        Button
      </button>
    </div>
  );
};

export default Button;
